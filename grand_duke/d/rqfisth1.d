BEGIN ~rqfisth1~

IF ~RandomNum(4,1)~ THEN BEGIN Annyoed
  SAY ~Why do you pester us?~
  IF ~~ THEN EXIT
END

IF ~RandomNum(4,2)~ THEN BEGIN Surely
  SAY ~I think I will explore this place later when my shift ends.~
  IF ~~ THEN EXIT
END

IF ~RandomNum(4,3)~ THEN BEGIN IpsoFacto
  SAY ~We would have killed Krivy, but Belt wants him alive for some reason.~
  IF ~~ THEN EXIT
END

IF ~RandomNum(4,4)~ THEN BEGIN Bored
  SAY ~So much fist for this one, man. Sigh.~
  IF ~~ THEN EXIT
END

